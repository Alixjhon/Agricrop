{
  "name": "agricrop-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Root package.json for Ionic Appflow — uses npm workspaces to install the Capacitor project in /frontend",
  "workspaces": [
    "frontend"
  ]
}
